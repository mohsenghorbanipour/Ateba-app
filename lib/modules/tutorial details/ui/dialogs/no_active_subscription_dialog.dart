import 'package:flutter/material.dart';

class NoActiveSubscriptionDialog extends StatelessWidget {
  const NoActiveSubscriptionDialog({super.key});

  @override
  Widget build(BuildContext context) => Dialog(
    
  );
}